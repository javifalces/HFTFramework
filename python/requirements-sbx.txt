# Isolated requirements for sbx-rl (JAX-based RL policies).
#
# sbx-rl>=0.27 requires stable-baselines3>=2.9 which in turn requires numpy>=2.
# This conflicts with scikit-learn<1.4, skfolio<0.3 and numba in the main stack.
# Install in a dedicated virtual environment:
#
#   python -m venv .venv-sbx
#   source .venv-sbx/bin/activate   # Windows: .venv-sbx\Scripts\activate
#   pip install -r python/requirements-sbx.txt
#
sbx-rl>=0.27
stable-baselines3>=2.9
numpy>=2.0
gymnasium>=0.29.1
